services:
  gateway:
    image: reolink-sip-gateway:2.0.0-beta.1
    build:
      context: ../reolink_sip_gateway
      target: standalone
    network_mode: host
    restart: unless-stopped
    volumes:
      - gateway-data:/data
    stop_grace_period: 45s

volumes:
  gateway-data:
